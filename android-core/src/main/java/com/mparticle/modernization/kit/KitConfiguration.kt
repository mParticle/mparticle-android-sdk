package com.mparticle.modernization.kit

/**
 * Abstraction that should have the configuration for each kit (might be passed when creating the
 * instance of the kit or onKitCreate if resolved after the instance is created)
 */
class KitConfiguration {
}