name: "PR Reusable Checks"

on:
  pull_request:

jobs:
  security-testing:
    name: "Testing security workflow"
    uses: v-juliana/mparticle-workflows/.github/workflows/android-kit-pull-request.yml@stable
