include ':android-core'

include(':android-kit-base',
        ':kits:kahuna-kit',
        ':kits:adjust-kit',
        ':kits:comscore-kit',
        ':kits:kochava-kit',
        ':kits:branch-kit',
        ':kits:appboy-kit',
        ':kits:localytics-kit',
        ':kits:flurry-kit',
        ':kits:wootric-kit',
        ':kits:crittercism-kit',
        ':kits:tune-kit',
        ':kits:appsflyer-kit',
        ':kits:apptentive-kit',
        ':kits:appsflyer-kit',
        ':kits:foresee-kit'
)

project(':kits').getChildren().each {
    proj -> proj.name = 'android-' + proj.name
}

include(
        ':kits:all-kits'
)
project(':kits:all-kits').name = 'android-sdk'
