include (
        ':sdk'
)
project(':sdk').name = 'android-core'

include (
        ':kits:kahuna-kit',
        ':kits:adjust-kit',
        ':kits:comscore-kit',
        ':kits:kochava-kit',
        ':kits:branch-kit',
        ':kits:appboy-kit',
        ':kits:localytics-kit',
        ':kits:flurry-kit',
        ':kits:wootric-kit'
)
project(':kits').getChildren().each {
    proj -> proj.name = 'android-' + proj.name
}

include (
        ':kits:all-kits'
)
project(':kits:all-kits').name = 'android-sdk'